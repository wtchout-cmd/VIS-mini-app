# ── VIS TMS — Environment Variables ─────────────────────────────
# Copy to .env.local for dev, set as Vercel env vars for production

# Base URL of your n8n instance (no trailing slash)
VITE_N8N_BASE_URL=https://primary-production-c41f.up.railway.app

# Optional: webhook secret for added security (future use)
# VITE_WEBHOOK_SECRET=your-secret-here
